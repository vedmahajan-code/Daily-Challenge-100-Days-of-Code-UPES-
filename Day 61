/*Q111: Write a program to take an integer array arr and an integer k as inputs. 
The task is to find the first negative integer in each subarray of size k moving from left to right.
 If no negative exists in a window, print "0" for that window. Print the results separated by spaces as output.*/
#include <stdlib.h>
#define MAX 1000
void printFirstNegative(int arr[], int n, int k) {
    int queue[MAX], front = 0, rear = -1;
    for (int i = 0; i < n; i++) {
        // Add current element index if it's negative
        if (arr[i] < 0)
            queue[++rear] = i;
        // Remove elements out of the current window
        if (front <= rear && queue[front] <= i - k)
            front++;
        // Print result for current window
        if (i >= k - 1) {
            if (front <= rear)
                printf("%d ", arr[queue[front]]);
            else
                printf("0 ");
        }
    }
    printf("\n");
}
int main() {
    // Example usage:
    int arr1[] = {-8, 2, 3, -6, 10};
    int k1 = 2;
    printFirstNegative(arr1, sizeof(arr1)/sizeof(arr1[0]), k1);
    int arr2[] = {12, -1, -7, 8, -15, 30, 16, 28};
    int k2 = 3;
    printFirstNegative(arr2, sizeof(arr2)/sizeof(arr2[0]), k2);
    int arr3[] = {12, 1, 3, 5};
    int k3 = 3;
    printFirstNegative(arr3, sizeof(arr3)/sizeof(arr3[0]), k3);
    return 0;
}
