/*Q119: Write a program to take an integer array as input.
 Only one element will be repeated. Print the repeated element. Try to find the result in one single iteration.*/
#include <stdio.h>
int main() {
    int n;
    scanf("%d", &n);   // size of array
    int nums[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &nums[i]);
    }
    // Use a simple visited array (assuming values are non-negative and not too large)
    int visited[100000] = {0};  // adjust size if needed
    for (int i = 0; i < n; i++) {
        if (visited[nums[i]] == 1) {
            printf("%d\n", nums[i]);
            return 0;
        }
        visited[nums[i]] = 1;
    }
    return 0;
}
