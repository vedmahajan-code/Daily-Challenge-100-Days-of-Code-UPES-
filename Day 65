/*Q115: Write a program to take two strings s and t as inputs (assume all characters are lowercase).
 The task is to determine if s and t are valid anagrams, 
 meaning they contain the same characters with the same frequencies. Print "Anagram" if they are, otherwise "Not Anagram".*/
#include <stdio.h>
#include <string.h>
#include <stdbool.h>
#define CHAR_COUNT 26  // Only lowercase letters
bool isAnagram(char *s, char *t) {
    int count[CHAR_COUNT] = {0};
    // If lengths differ, they can't be anagrams
    if (strlen(s) != strlen(t))
        return false;
    // Count characters in s and subtract for t
    for (int i = 0; s[i] != '\0'; i++) {
        count[s[i] - 'a']++;
        count[t[i] - 'a']--;
    }
    // Check if all counts are zero
    for (int i = 0; i < CHAR_COUNT; i++) {
        if (count[i] != 0)
            return false;
    }
    return true;
}
int main() {
    // Sample Test Case 1
    char s1[] = "anagram";
    char t1[] = "nagaram";
    printf("%s\n", isAnagram(s1, t1) ? "Anagram" : "Not Anagram");
    // Sample Test Case 2
    char s2[] = "rat";
    char t2[] = "car";
    printf("%s\n", isAnagram(s2, t2) ? "Anagram" : "Not Anagram");
    return 0;
}
